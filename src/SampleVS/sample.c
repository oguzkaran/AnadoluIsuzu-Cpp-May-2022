#include <stdio.h>
#include <stdlib.h>
#include <time.h>

void func(double b)
{
	long *p = 0;
}

#if 0

int main()
{
	srand((unsigned int)time(NULL));
	int* p = foo(10, 20);

	printf("%d\n", *p);

	return 0;
}
#endif
